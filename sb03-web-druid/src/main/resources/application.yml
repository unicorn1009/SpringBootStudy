spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ssm
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver
    filters: stat  # DataSource类中没有这个属性,所有没提示, 但是我们放入的DruidDataSource这个bean有这个属性, 所以会绑定成功并且生效