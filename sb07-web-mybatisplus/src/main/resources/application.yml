spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ssm?characterEncoding=utf-8&useSSL=false
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver

server:
  port: 8007

# mybatis-plus配置好了默认的mapper文件扫描路径(classpath:mapper/**/*xml)
#mybatis-plus:
#  mapper-locations: classpath:mapper/*.xml